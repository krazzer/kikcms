{% extends '@kikcms/datatable/window.twig' %}

{% block headerTitle %}
    {% set label = labels[0] ~ (objectName ? ' "' ~ objectName ~ '"') %}
    {{ tl('dataTable.' ~ (canEdit ? 'edit' : 'view'), {itemSingular: label})|ucfirst }}
{% endblock %}
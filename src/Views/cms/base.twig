{# @var menuStructure \KikCMS\Services\Cms\CmsMenuGroup[] #}
{# @var menuGroup \KikCMS\Services\Cms\CmsMenuGroup #}
{# @var menuItem \KikCMS\Services\Cms\CmsMenuItem #}

{% extends '@kikcms/base.twig' %}

{% block content %}
    <div id="menu">
        <a class="logo" href="{{ route('cms') }}"></a>

        {% for menuGroup in menuStructure %}
            <div class="menu-group">
                <div class="title">
                    {{ menuGroup.label }}
                </div>
                {% for menuItem in menuGroup.menuItems %}
                    {% set class = menuItem.id == actionName or menuItem.id == selectedMenuItem ? ' selected' : '' %}
                    <a class="menu-item{{ class }}" href="{{ url(menuItem.route) }}">
                        {{ menuItem.label }}
                    </a>
                {% endfor %}
            </div>
        {% endfor %}
    </div>
    <div id="main">
        <div id="main-content">
            {% if flash %}
                {{ flash.output }}
            {% endif %}
            {% block cmsContent '' %}
        </div>
    </div>
{% endblock %}
{% extends '@kikcms/login/base.twig' %}

{%- block title -%}
    Login
{%- endblock -%}

{% block loginContent %}
    <div id="login-form">
        {{ form|raw }}
    </div>

    <a class="lostpass" href="{{ url('cms/login/reset') }}">{{ tl('login.lostLinkText') }}</a>
{% endblock %}
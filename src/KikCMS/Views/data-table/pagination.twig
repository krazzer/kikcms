{% for page in pagination.pages %}
    {% if page %}
        <li class="{{ page == pagination.current ? 'active' : '' }}"><a>{{ page }}</a></li>
    {% else %}
        <li class="disabled"><a>...</a></li>
    {% endif %}
{% endfor %}

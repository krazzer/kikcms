<phpunit>
    <filter>
        <whitelist processUncoveredFilesFromWhitelist="true">
            <directory suffix=".php">Controllers</directory>
            <file>.</file>
        </whitelist>
    </filter>
</phpunit>